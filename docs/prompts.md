# Custom prompts

For an overview of custom prompts, see [this documentation](https://developers.openai.com/codex/custom-prompts).

Custom prompts can be invoked with `/prompts:<name>` anywhere Codex accepts input,
including the TUI, interactive CLI, and exec mode (for exec, pass the slash command
as the prompt argument or via stdin).
